spring:
  cloud:
    config:
      server:
        native:
          search-locations: tmsc-config
        git:
          uri: https://github.com/Komdosh/TenMinutesCloudStreaming
          searchPaths: tmsc-config
